# AC Server Dashboard — Python-Abhängigkeiten
# Getestet auf Python 3.11 / Debian 12 (Bookworm)
# Generiert mit: pip freeze | grep -E "^(Flask|Werkzeug|Jinja2|click|blinker|itsdangerous|MarkupSafe|psutil)=="

Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.4.1
blinker==1.9.0
itsdangerous==2.2.0

# Systemmetriken (CPU, RAM, Netzwerk)
psutil==7.2.2
